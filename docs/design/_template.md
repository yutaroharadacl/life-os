# 詳細設計書：<機能名>

| 項目       | 内容                                      |
| ---------- | ----------------------------------------- |
| 対象要件   | 要件定義書「5-X. ○○機能」                 |
| タスクID   | `docs/tasks.md` の T-XX（あれば）         |
| 作成日     | YYYY-MM-DD                                |
| ステータス | 未着手 / 実装中 / 実装済み / レビュー済み |

> このテンプレートの全セクションを埋めること。該当なしの場合も「なし」と明記し、セクションごと削除しない。
> `design-impl-reviewer` はこの構造を前提に実装と突合する。

---

## 1. 概要

この機能で何を実現するか。要件定義書の該当章から要求を引用し、1〜3段落で説明する。

## 2. スコープ

### やること

- （箇条書き）

### やらないこと

- （箇条書き。フェーズ2に送るもの、別タスクにするものを明示する）

---

## 3. 画面・UI 仕様

対象画面（要件定義書「8. 画面要件」の画面番号を参照）。

### 画面構成

- 画面名 / ルート（例: `/inventory/lists`）
- 配置する UI 要素の一覧と役割

### 見出し階層

**必ず埋めること。** 各見出しの HTML タグレベルを明記する。
1ページに `<h1>` が必ず1つ存在し、レベルが飛ばないようにする（`h1` → `h3` は不可）。

| 見出しテキスト | レベル | 備考               |
| -------------- | ------ | ------------------ |
| 例: 在庫一覧   | `<h1>` | この画面の主見出し |
| 例: 冷蔵庫     | `<h2>` | 保管場所ごと       |

### 状態

| 状態         | 表示内容 |
| ------------ | -------- |
| 初期表示     |          |
| ローディング |          |
| 0件          |          |
| エラー       |          |

### 操作と遷移

- ユーザー操作 → 起きること → 遷移先

---

## 4. データ・型定義

実際に定義する TypeScript の型をそのまま書く（実装時にコピーできる粒度で）。

```ts
export type Xxx = {
  // ...
};
```

- 配置先ファイル: `src/features/<feature>/types/index.ts`
- 要件定義書「6. データ要件」との対応（項目名・必須／任意）を明記する

---

## 5. コンポーネント設計

### 新規／変更するファイル

| ファイルパス                                            | 新規/変更 | 責務 |
| ------------------------------------------------------- | --------- | ---- |
| `src/features/<feature>/components/Xxx/Xxx.tsx`         | 新規      |      |
| `src/features/<feature>/components/Xxx/Xxx.module.scss` | 新規      |      |
| `src/features/<feature>/components/Xxx/index.ts`        | 新規      |      |
| `src/app/<route>/page.tsx`                              | 変更      |      |

> ここに列挙されていないファイルを実装時に勝手に増やさない。必要になったら本設計書を先に更新する。

### 階層図

```
<Page>
└── <FeatureComponent>
    └── <ChildComponent>
```

### Props

各コンポーネントの `Props` 型を記述する。

---

## 6. 状態管理・データフロー

- **Server Component / Client Component の分担**（`'use client'` を付けるコンポーネントを明示）
- **サーバー状態**: TanStack Query を使うか、現段階はモックデータの直接読み込みか
- **クライアント状態**: Zustand を使う場合はストアの形と配置先
- **レンダリング戦略**: 静的プリレンダリングでよいか、リクエストごとの評価が必要か。
  現在日時・リクエスト情報に依存する表示がある場合は `export const dynamic = 'force-dynamic'`
  （または `revalidate`）の要否をここで明記する
- データの流れ（取得 → 加工 → 表示）

---

## 7. API 仕様

> Go バックエンドは未着手。現段階はモック実装だが、将来の API 契約を見据えて入出力を定義する。

| メソッド | パス       | 用途 |
| -------- | ---------- | ---- |
| GET      | `/api/xxx` |      |

### リクエスト / レスポンス

```ts
// Request
// Response
```

### 現段階のモック実装

- 配置先: `src/features/<feature>/api/`
- モックデータの持ち方（ハードコード配列 / 遅延あり など）
- **日付を含むモックは、絶対日付をハードコードせず当日からの相対で生成する**
  （例: `shiftDays(-2)`）。絶対日付だと日が経つにつれて
  「期限切れ・期限間近・余裕あり」などの検証したいパターンが崩れ、動作確認に使えなくなる
- モックが再現すべき状態のパターンを列挙する（例: 期限切れ / 期限間近 / 余裕あり / 期限なし）

---

## 8. バリデーション・エラーハンドリング

| 対象 | ルール | エラーメッセージ（日本語） |
| ---- | ------ | -------------------------- |
|      |        |                            |

- 想定するエラーケースと、その時のユーザーへの見せ方

---

## 9. テスト観点

> このセクションが `unit-test-writer` の入力になる。抽象的に書かず、
> 「入力 → 期待される結果」が読み取れる粒度で書くこと。

### 正常系

- [ ]

### 異常系

- [ ]

### 境界値

- [ ] 0件のとき
- [ ] 任意項目が未入力（null / 空文字）のとき
- [ ]

---

## 10. 未決事項・確認事項

> 設計時に判断できなかったことを勝手に決めず、ここに挙げてユーザーに確認する。
> **すべて解決してから実装フェーズに進む。**

- [ ] （未決事項）
